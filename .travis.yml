language: node_js
node_js:
  - 6
cache:
  directories:
    - node_modules
    - functions/node_modules
branches:
  only:
  - master
script:
  - npm run build
  - npm test -- --maxWorkers=2
  - cd functions && npm install && npm test -- --maxWorkers=2
